# 首页 Hero 气球外观与响应式验证

日期：2026-09-09（Asia/Shanghai）。本任务未使用 skill。

## 变更

- 气球改为顶部饱满、底部平滑收拢的贝塞尔轮廓，增加柔和高光、弧形反光、绳结与渐隐细绳；字母按气球大小缩放。
- 五色数据、气球位置、物理模拟、指针交互、离屏暂停与减少动画逻辑保持原有行为。渐变使用组件实例唯一 ID。
- 扫描线置于气球后方，避免穿过字母。
- 将 Hero 的三档响应式规则从全局样式迁入首页样式，解决页面样式延迟加载覆盖窄屏布局的问题；手机画框调整为 1.65 宽高比。
- 更新已有气球结构断言，新增浏览器测试检查五种屏宽下首次加载与路由返回时的文案、面板边界和气球完整性。

## 验证

- 第一版完成后运行全量 `npm test`：107 个测试文件、670 项测试通过。最终视觉与 CSS 调整后复跑首页相关 7 项测试，全部通过。
- 最终 `npm run lint`、`npm run build`、构建资源预算、许可证与站点元数据检查通过。
- 使用本机 Google Chrome 152.0.7977.83 和本地 Vite e2e 模式，检查 390、768、1024、1440、1920px：文案与面板均在视口内，五只气球均在画框内，无页面横向溢出。
- Chrome 中漂浮、指针悬停、动态切换减少动画与“新手入门”导航正常；无控制台 warning/error 或页面异常。
- 新增 `e2e/home-hero.spec.ts` 的 5 项测试通过，每项同时检查首次加载与路由返回。Chrome 运行配置保存在忽略目录 `.codex-tmp/hero-playwright.config.ts`。
- 对比截图与最终截图保存在忽略目录 `.codex-tmp/hero-balloons-before/`、`.codex-tmp/hero-balloons-final/`，只作为本地视觉证据。
- 根操作文档及本任务文件的格式检查、`git diff --check` 通过。
- 构建仍提示既有 AssistantPage 分块超过 500 kB；首页入口资源预算通过。本任务未修改该模块。

## 交接

任务：首页 Hero 气球外观优化及相关响应式修正。

当前分支 / 提交：`astra` / `70a6668`。

已完成：外观、扫描线层级、手机布局修正及本地验证。

生产状态：未提交、推送或部署。冷启动只读查询时最近 Pages run `34306437973` 成功；本次效果仅在本地验证。

未提交或用户已有改动：保留任务前的 `PRIVACY.md`、`ROADMAP.md` 修改；本任务修改 Hero 组件、首页及全局样式、首页测试，新增浏览器测试和本文。

遗留风险：未在生产发布；完整跨浏览器 E2E 矩阵未运行，本次浏览器验收使用 Chrome。

回滚方式：仅反向应用本任务源码、测试与本文的差异，保留用户原有修改。

下一步：本地修改已就绪，后续提交与发布按明确授权执行。

需要项目负责人输入或批准：无。
