# 学习页排版与 UI 优化验收

日期：2026-09-09（Asia/Shanghai）。分支：`astra`，基准提交：`70a6668`。

## 范围与最终效果

- 根据用户要求，全程不使用任何 skill。学习页首屏保持浅色，沿用绿白配色、浅网格背景与小圆角控件。
- 调整标题与章节排版，正文最大宽度为 1280px，减少侧栏与章节标题占用的横向空间。
- 增加「开始四周计划」页内入口；起点选中项显示勾选标记，四周进度显示已完成任务数。
- 阶段说明减少卡片嵌套和移动端左侧缩进，训练节奏使用连续细线分区。
- 修复起点和训练节奏图标被内边距压缩的问题；知识地图名称允许换行，数量信息独立排列。
- 保留全部学习阶段、任务、资源地址、推荐逻辑与进度存储契约。未修改路线数据、首页样式或气球实现。

## 验证

- 全量 `npm test`：107 个测试文件、670 项测试通过。
- 最终浅色版本保留原文案后，学习页定向单元测试再次通过：6 项。
- `npm run lint` 通过；新增 E2E 与首页往返测试另行执行 ESLint，通过。
- `npm run build` 通过，包括 TypeScript、许可证、站点元数据与资源体积门禁。既有 AssistantPage 分块超过 500 kB 的提示仍存在。
- 使用本机 Google Chrome，经 Playwright 的 `channel: 'chrome'` 执行；本地 Vite 使用 `e2e` 模式，不作为生产数据验证。
- 15 项 Chrome E2E 通过：7 项学习页布局与交互、3 项既有学习页回归、5 项首页气球往返回归。
- 学习页覆盖 390、820、1024、1440、1920px，检查页面与关键内容边界，避免 `overflow: clip` 掩盖列溢出。
- 交互覆盖首屏计划入口、推荐平台联动、键盘切换周次、任务勾选、刷新后进度保留、重置、阶段展开、知识地图切换与移动目录定位。
- 390px 与 1440px 的 axe WCAG A/AA 自动扫描为零违规，回归未捕获页面运行错误。
- 实际截图检查桌面、移动端与宽屏。最终浅色版截图在 `.codex-tmp/learning-light/`，移动端章节视口截图在 `.codex-tmp/learning-final/mobile-view-*.png`；均为忽略的本地验证产物。
- 变更文件 Prettier 检查及 `git diff --check` 通过。

首页 E2E 原先只等待 learning URL，可能在懒加载页面完成渲染前点击返回，导致布局断言遇到临时卸载节点。此次仅增加学习页标题和返回 URL 等待，保留所有气球布局断言。

## 交接

任务：学习页浅色排版与 UI 优化。

当前分支 / 提交：`astra` / `70a6668`，修改尚未提交。

已完成：上述页面调整、交互与视觉验收。

生产状态：未提交、推送或部署。冷启动只读观察到 Pages run `34306437973` 成功，SHA 为 `8a175816913b7350f46071c964ba326d34600e40`。

未提交或用户已有改动：用户的 `PRIVACY.md`、`ROADMAP.md` 以及此前气球源码、首页 CSS、全局 CSS、首页单测和气球证据文件 SHA-256 与任务开始时一致。此前新增的 `e2e/home-hero.spec.ts` 仅补充两行导航等待。

遗留风险：中文衬线标题沿用本机字体回退，不同操作系统字形可能不同；无新增依赖或第三方数据流。

回滚方式：单独撤销本次 LearningPage、learning.css 与对应 E2E/证据改动，保留既有气球和用户改动；不使用整工作树重置。

下一步：等待用户评审本地效果；没有发布授权。

需要项目负责人输入或批准：无。
