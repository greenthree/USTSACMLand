# USTS ACM Land 隐私说明

更新日期：2026 年 9 月 8 日

本说明适用于 USTS ACM Land 当前提供的账号、成员资料、竞赛数据、每日题目、训练记录和线上比赛日历服务。生产配置、数据表和第三方处理边界如有变化，会同步更新本说明及[第三方数据来源说明](./docs/third-party-data-sources.md)。

## 1. 收集和保存的数据

### 账号与成员资料

- 注册使用邮箱、密码和姓名；认证由 Supabase Auth 处理，本项目业务表不读取或保存密码明文。
- 成员资料包括姓名、QQ、年级、专业和竞赛平台账号。
- 浏览器会在当前设备保存未提交的资料草稿，保存成功、退出或注销后清除。

### 训练与竞赛数据

- 同步服务保存 Rating、通过题数、更新时间、历史快照和结构化错误状态。
- 每日题目记录包括题目链接、标题、难度、标签、训练提示、完成状态和讨论记录。
- 线上比赛日历只保存白名单平台的比赛名称、平台、开始/结束时间和官方链接，不保存成员信息。

## 2. 公开范围与访问边界

注册后需先完成邮箱确认，之后才能登录。姓名、年级和专业填写完整且成员保持公开状态时，已验证的平台账号、Rating、通过题数、数据更新时间、规范化头像，以及由成功历史快照计算的时间范围刷题增量会显示在公开页面。

邮箱、QQ、密码、登录令牌、同步错误原文和后台审计详情不进入公开榜单。头像公开地址只使用成员 UUID，不包含 QQ。管理员停用成员后，其资料和头像会从公开页面隐藏并停止同步，但现有账号和业务数据继续保留，直到恢复或永久注销。

普通成员只能访问自己的账号、资料、训练记录和可导出数据；管理员操作受角色、RLS、审计和最小权限规则约束。

## 3. 数据用途

- 确认集训队成员身份并管理平台绑定。
- 生成队内 Rating 榜、刷题榜、成员详情和训练进度。
- 提供每日题目、成员完成记录和队内讨论。
- 展示近期公开线上比赛及其官方链接。
- 执行定时同步、故障诊断、安全审计和滥用防护。

## 4. 每日题目与讨论

每日题目由管理员维护，题面仍保留在对应竞赛平台，本网站只保存题目链接、标题、难度、标签和训练提示。已登录且账号启用的成员可以记录完成状态并参与讨论；讨论作者只在成员范围内显示，管理员可以隐藏不合适的内容。

访客可以查看已发布且已到日期的题目，但不能读取成员完成明细或讨论内容，公开页面只展示匿名聚合数量。

## 5. 第三方服务

- **GitHub Pages 与 Supabase**：GitHub Pages 托管静态前端，Supabase 提供认证、数据库和服务端函数。
- **竞赛平台**：同步服务会使用成员填写的平台账号查询 Codeforces、牛客、AtCoder、AtCoder Problems、XCPC ELO、洛谷和 QOJ。
- **clist.by**：线上比赛日历由服务端使用受保护的官方 API 凭据读取；服务端按白名单过滤平台和官方链接，凭据不会进入前端或日志。
- **Firecrawl**：仅用于牛客反自动化回退查询和 QOJ 临时登录。QOJ 请求使用专用服务账号和目标用户名，不发送成员邮箱、QQ 或本站密码；临时浏览器关闭会话录制和直播，不使用持久 profile，每次 attempt 后主动删除会话。
- **腾讯 QQ 头像接口**：成员填写或修改 QQ 后，只有服务端会发送 QQ 号以获取头像；浏览器不会取得该请求地址，本站只在私有存储中保留规范化 WebP 头像和不可逆来源摘要。

各服务对请求数据的处理同时受其自身政策和生产配置约束。完整字段、认证方式和保留边界见[第三方数据来源说明](./docs/third-party-data-sources.md)。

## 6. 数据导出

登录成员可以在“我的资料”中下载版本化 JSON 文件。导出内容包括本人账号和资料、平台绑定、当前统计与历史快照、同步记录、每日一题完成和讨论记录，以及其他由当前数据导出接口提供的本人数据。被管理员隐藏但仍在保留期内的本人讨论会标明状态后随文件返回。

导出接口只绑定当前登录账号，不接受目标成员 ID；管理员使用该入口时同样只能导出自己。文件不包含密码、登录令牌、服务密钥、内部租约令牌、管理员身份字段或其他成员数据。导出文件由浏览器直接下载，本站不会另存一份导出副本。

## 7. 修改与删除

成员可以在“我的资料”中更正资料和非 XCPC 平台账号；平台账号变更后需要重新验证。普通成员可在账号页再次验证当前密码并二次确认后永久注销本人账号，无需由管理员代删。管理员账号必须先完成权限交接和降级，再按普通成员流程注销；需要停用账号或处理错误绑定时，可通过集训队内部渠道联系管理员。

永久注销会删除 Auth 用户、资料、平台绑定、当前统计、历史快照和同步记录，并清除相关审计记录中的个人标识，只保留不可关联成员的匿名删除事件。正在运行同步时，注销会整体失败，不会先删除部分数据。

删除前，服务端还会在数据库之外保存一个不含成员身份的恢复下限时间；该记录失败时不会执行删除。Supabase、GitHub 和 Firecrawl 的既有备份与日志可能按生产配置或服务政策继续保留，本站不能改写已经生成的供应商备份，但禁止把早于最新注销恢复下限的备份恢复上线。详细规则见[账号与数据生命周期](./docs/data-lifecycle.md)、[数据库备份与恢复方案](./docs/backup-and-recovery.md)和[生产运维手册](./docs/operations-runbook.md)。

## 8. 安全边界与问题反馈

本项目仅用于集训队训练与展示，不是学校统一身份系统。请勿复用其他网站的密码，也不要在平台账号字段中填写 Cookie、Token 或其他秘密。

若发现数据泄露或越权问题，请不要在公开 Issue 中附带敏感信息，应通过 GitHub 私密漏洞报告或集训队内部渠道联系维护者。
