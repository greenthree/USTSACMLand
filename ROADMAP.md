# USTSACMLand 开发路线图

最后更新：2026-09-03（Asia/Shanghai）

本路线图只保留仍需执行的优化任务、发布门禁、运维演练与当前生产基线，所有已经完成并上线的历史功能、数据库迁移、测试套件与历史烟测记录已从本文清退；详细实现记录参见 `README.md`、`docs/evidence/`、`docs/operations-runbook.md` 和 GitHub Pull Request。

勾选规则：

- `[ ]`：待开发、技术债务治理、配置、生产演练或人工确认。
- `[x]`：功能或优化已部署到生产，并完成与风险相匹配的验收。

---

## 1. 产品范围

USTSACMLand 的定位是苏州科技大学 ACM 集训队官网，当前正式产品范围包括：

- 集训队与算法竞赛介绍、校内赛事入口、新手学习路线与入门资源；
- 每日一题、完成记录与成员讨论；
- Codeforces、牛客、AtCoder、XCPC ELO Rating 榜；
- Codeforces、牛客、AtCoder、洛谷、QOJ 刷题榜与增量榜；
- 成员注册、登录、密码找回、资料维护、平台绑定和个人数据导出；
- 管理员成员管理、平台账号维护、同步中心、手工数据维护、数据源健康与审计；
- 训练目标，以及围绕现有训练、榜单和成员管理能力的工程与体验改进。

### 明确取消的未来用户功能

以下用户功能已从路线图永久移除，不再安排后续产品开发：

- WebChat / AI 学习助手，包括图片输入、视觉模型、缓存探针、成员额度和历史会话。
- 推荐计划，包括邀请码、绑定、奖励额度、全局开关重开和生产并发验收。
- CF / AtCoder Rating 曲线与比赛明细。
- 过题明细浏览、题目难度分布和标签统计。
- 连续活跃天数、比赛提醒、群机器人播报、战队训练计划和移动端原生应用。

WebChat 与推荐计划的既有前端、Edge Function、数据库 migration、测试和安全实现全部保留，不删除、不回滚，但退出当前及未来产品范围。生产开关保持关闭，只有项目负责人未来明确重新立项后，才允许恢复开发或进行重开验收。

---

## 2. 当前生产基线概览

当前系统已在正式站点 <https://ustsacm.fun/> 稳定运行，具备以下已验收的基础能力：

- **静态交付与网络边缘**：GitHub Pages 托管 React SPA，Cloudflare 提供 DNS、强制 HTTPS、CNAME Flattening、SPA 回退以及静态指纹资源一年期长期缓存；
- **认证与账号系统**：Supabase Auth 邮箱注册、密码登录、真实邮箱找回密码、修改密码、会话恢复、资料修改与受控自助注销；
- **平台同步流水线**：覆盖 Codeforces、牛客、AtCoder、洛谷、QOJ 与 XCPC ELO；日更平台每天北京时间 07:00 / 19:00 更新，周更平台每周二 08:00 更新；所有同步在首次失败后最多自动重试一次，失败平台保留最后成功统计；
- **管理与审计中枢**：管理员成员管理、平台账号验证、全量/定向同步触发、手工分数与题数维护、审计日志与数据源健康状态监控；
- **训练与交流功能**：每日一题打卡与讨论审核发布、训练目标周期与进度跟踪、个人数据合规导出；
- **数据与安全保障**：76 个生产 migration 与 RLS 安全围栏部署，加密数据库定时备份与隔离恢复演练闭环。

---

## 3. 前端工程与用户体验优化

围绕代码架构、性能瓶颈、交互稳定性和安全防御，按优化优先级推进以下工作：

### P0：核心交互稳定性与缺陷修复

- [x] **每日一题打卡消除整页跳闪**
  - **目标**：重构 `DailyProblemPage.tsx` 中的打卡与取消打卡交互。
  - **方案**：由全量触发 `loadProblem()` 重置页面，改为局部/乐观更新 `problem.completedAt` 与 `problem.completedCount`，并同步更新归档列表；消除整页骨架屏卸载、跳闪与滚动位置丢失。
- [x] **成员详情页修复未绑定平台外链**
  - **目标**：修正 `MemberPage.tsx` 中平台链接的渲染逻辑。
  - **方案**：当成员未绑定某个平台（`externalId` 为空）时，渲染静态文本标签，杜绝渲染包含 `https://codeforces.com/profile/` 等基础路径的空超链接。

### P1：数据表现与安全加固

- [x] **登录重定向路径安全加固**
  - **目标**：提升 `LoginPage.tsx` 对 `returnTo` 查询参数的防御纵深。
  - **方案**：收紧重定向路径白名单校验，过滤形如 `//evil.com`（协议相对路径）或 `/\\evil.com` 等路径变体，杜绝开放重定向（Open Redirect）风险。
- [x] **榜单排序多维浮点重算缓存**
  - **目标**：优化 `RankingsPage.tsx` 与 `src/lib/rankings.ts` 的排序与名次变化计算性能。
  - **方案**：采用 Map 预计算综合评分与名次变动基准，避免在 `sort` 比较器与名次比对循环中重复执行包含 `flatMap`、基准值除法、`reduce` 与 `Math.sqrt` 的 `calculateOverallRating` 复合计算，将浮点与数组开销降为 $\mathcal{O}(N)$ 预处理 + $\mathcal{O}(N \log N)$ 纯数值比较。
- [x] **成员列表页补齐加载态与空状态**
  - **目标**：完善 `MembersPage.tsx` 的状态反馈。
  - **方案**：消费 `useMembersData().loading` 接入加载骨架屏；在搜索过滤无匹配成员或数据拉取失败时渲染统一的 `EmptyState` 友好提示，并补齐单测。

### P2：样式架构重构与组件解耦

- [x] **`styles.css` 模块化拆解与冗余层叠清理**
  - **目标**：将 7,594 行、139 KB 的单体全局 CSS 进行解耦瘦身。
  - **方案**：将首页专用样式（物理气球动画、仪器面板、扫描线等）提取为独立的 `homepage.css` 按需引入；清理文件尾部多层特异性覆盖（Specificity Wars）的样式补丁，将首屏入口 CSS 体积从 109 KB 降至安全裕度内（当前预算上限 128 KB）。
- [x] **`AccountPage` 表单状态下沉与解耦**
  - **目标**：治理 `AccountPage.tsx` 顶层 35+ 个 `useState` 集中管理与深层 Prop Drilling。
  - **方案**：将修改密码、账号注销、数据导出等独立表单的状态与提交逻辑收敛至各自子组件内，消除父组件高频全量重新渲染。
- [x] **原生 `window.confirm` 替换为无障碍模态框**
  - **目标**：统一全站弹窗交互风格，消除阻塞式原生弹窗。
  - **方案**：将 `TrainingGoalsPage`（归档目标）、`AdminDailyProblemsPage`（删除草稿）、`AdminFirecrawlKeysPanel`（删除密钥）等处的 `window.confirm` 替换为项目统一视觉规范的模态对话框，复用 `useDialogFocus` 维持焦点无障碍闭环。
- [x] **子路由级 Error Boundary 保护**
  - **目标**：增强单页面容错边界。
  - **方案**：在 `AppShell.tsx` 的 `<Outlet />` 外层增设路由级错误边界，防止局部表格或页面渲染异常导致顶部导航栏和整个站点外壳白屏崩溃。
- [x] **公开榜单增加客户端主动刷新与重试入口**
  - **目标**：改善榜单数据新鲜度与弱网恢复能力。
  - **方案**：在 `MembersDataContext` 中对外暴露 `reload` 方法，并在榜单更新时间旁提供手动刷新按钮与网络失败重试入口，免除用户强制 F5 整页重载。

### P3：代码精简与主题体验探索

- [x] **超长单文件组件拆分与数据外置**
  - **目标**：降低核心超大组件的维护成本。
  - **方案**：拆解 `FreshmanContestPage.tsx`（1,453 行）、`HomePage.tsx`（1,082 行）与 `LearningPage.tsx`（906 行），将大型静态文案、知识图谱与比赛模拟数据提取为独立的配置数据文件。
- [x] **注册页清理已归档特性的切屏探针请求**
  - **目标**：减少无效网络请求。
  - **方案**：清理 `RegisterPage.tsx` 中由 `visibilitychange` 和 `focus` 触发的 `checkReferralCodeAvailability` 自动轮询请求。
- [x] **深色主题（Dark Mode）适配探索**
  - **目标**：改善夜间训练与刷题场景下的视觉舒适度。
  - **方案**：基于现有 CSS 变量体系扩展 `@media (prefers-color-scheme: dark)` 或主题切换支持。

---

## 4. 发布治理与生产运维待办

- [ ] **完成最终检查并创建 `v1.0.0` 标签**
  - 按 `docs/release-checklist.md` 补齐 Auth 频控处置事实与供应商日志说明，完成全量生产门禁核验后正式创建 `v1.0.0` 版本标签。
- [ ] **旧地址跳转回滚演练**
  - 确认 `greenthree.github.io/USTSACMLand/` 自动跳转到正式域名，并在明确的维护窗口按运行手册验证 DNS 切换与回滚步骤。
- [ ] **公开文档链接核对更新**
  - Cloudflare 自定义域名接入完全稳定后，对 README、隐私说明、运维文档及所有外部公开链接进行统一核对更新。
- [ ] **自然训练进度达标“确认完成”流程验证**
  - 使用自然训练进度达标的真实同步快照，在生产环境完成一次普通成员“确认完成”UI 流程验证与历史归档核对。

---

## 5. 已暂停并归档的遗留模块

WebChat（含图片输入）与推荐计划已正式退出产品范围：

- 生产数据库与构建配置保持固定关闭：`CHAT_ENABLED=false`，`VITE_WEBCHAT_UI_ENABLED=false`，`VITE_WEBCHAT_IMAGE_INPUT_ENABLED=false`，推荐计划全局开关关闭；
- 保留其现有代码、数据库 Schema、函数实现、测试用例和注销级联逻辑，以维持数据库兼容性与安全边界；
- 日常维护工作仅限于关闭态安全检查、数据备份与注销回归，严禁向普通成员开放入口或重新打开生产开关。

---

## 6. 完成定义

任一任务只有同时满足以下条件才可标记完成：

- 行为符合验收标准，正常、空数据、失败和无权限状态均有合理处理；
- 关键逻辑与算法具备完善的自动化测试与回归保障；
- 不引入未授权数据访问、敏感信息泄漏或越权操作；
- 文档、类型定义与生产配置与实现保持完全同步；
- 在生产构建门禁（`npm test`、`npm run lint`、`npm run build`）中全面通过；
- 已完成的任务经核验后从 `[ ]` 改为 `[x]`。

---

## 7. 主要风险与应对措施

| 风险                            | 影响                            | 应对措施                                                             |
| :------------------------------ | :------------------------------ | :------------------------------------------------------------------- |
| 第三方平台限流、结构变化或封禁  | 榜单数据同步延迟或失败          | 每次最多自动重试一次、固定样本契约测试、后台健康监控、保留最后成功值 |
| QOJ / Firecrawl 登录或额度异常  | 周更数据不可用或产生额外成本    | Vault 多 Key 轮换、额度监控、单次重试上限、会话自动清理              |
| 重试与队列重复领取              | 重复上游请求或旧结果覆盖新结果  | 原子领取机制、attempt fencing、幂等快照与明确终态                    |
| 账号注销跨系统级联失败          | Auth 与业务数据或备份下限不一致 | 租约保护、共享事务锁、失败关闭与双重只读对账                         |
| 遗留 WebChat 或推荐计划被误开启 | 暴露已废止入口或产生非预期费用  | 保持函数与构建三层开关锁死，巡检仅验证关闭态安全                     |
| 单体样式重构导致非预期视觉漂移  | 页面布局在移动端或特定视口破损  | 按页面渐进拆分样式，使用 Chrome 覆盖桌面、390px 与宽屏视觉回归       |
