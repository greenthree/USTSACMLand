# Curated runtime license materials

These files are used only when an exact production package version is included in the Vite bundle but its npm tarball omits package-level license material. The generator maps each file to an exact `name@version` and fails closed for any other version.

- `ai-sdk-provider-utils-5.0.10-LICENSE.txt` is the root `LICENSE` at Vercel AI tag `ai@7.0.30` (`2667d274aa5d4ed53ce3fcbaa020302f47a86ec2`), the release containing `@ai-sdk/provider-utils@5.0.10`. GitHub blob SHA: `6c16c29f41168411dbf6e3ba3a063ce789c27334`.
- `use-composed-ref-1.4.0-LICENSE.txt` records the MIT declaration and standard permission terms for the exact npm package and source tag `v1.4.0` (`ea2c9d969f2f90a349f4c99338b806423824cea2`). The upstream tag and npm tarball contain no LICENSE file or copyright notice, so the material states that absence explicitly and does not infer an attribution from another package. This exception must be re-reviewed if the dependency changes.
